{
  "name": "cip-olinda-app",
  "version": "0.1.0",
  "description": "Mapa Rizomático — orquestrador administrativo da pesquisa do CIP Olinda (OCCA)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
